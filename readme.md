# Система розпізнавання об'єктів на базі Faster R-CNN

##  Опис проєкту

Повнофункціональна система розпізнавання об'єктів, яка використовує двоступеневий детектор **Faster R-CNN** з backbone ResNet50-FPN. Система забезпечує:

-  Завантаження та обробку зображень
-  Детекцію об'єктів з візуалізацією
-  Обчислення метрик якості (IoU, mAP, Precision, Recall, F1-score)
-  Навчання та донавчання моделі
-  Автоматичну генерацію звітів
-  Багатофайлову модульну архітектуру

## Архітектура проєкту


faster_rcnn_system/
├── config.py              # Конфігурація системи
├── utils.py               # Допоміжні функції
├── model.py               # Faster R-CNN модель
├── visualizer.py          # Візуалізація результатів
├── metrics.py             # Обчислення метрик
├── dataset.py             # Робота з датасетами
├── trainer.py             # Навчання моделі
├── report_generator.py    # Генерація звітів
├── main.py               # Головний модуль
└── README.md             # Документація
└──converter.py            #Конвертація Анотацій


##  Швидкий старт

### 1. Демонстраційний режим

Запуск повної демонстрації можливостей системи:


python main.py --mode demo


Цей режим:
- Створює тестовий датасет
- Виконує детекцію об'єктів
- Будує візуалізації
- Генерує повний звіт

### 2. Детекція об'єктів на зображенні


python main.py --mode detect --image path/to/image.jpg --threshold 0.5


Параметри:
- `--image`: шлях до зображення
- `--threshold`: поріг впевненості (за замовчуванням 0.5)
- `--checkpoint`: шлях до збереженої моделі (опціонально)

### 3. Навчання моделі


python main.py --mode train --data-dir ./data --epochs 10 --batch-size 4 --lr 0.005


Параметри:
- `--data-dir`: директорія з даними
- `--epochs`: кількість епох
- `--batch-size`: розмір батчу
- `--lr`: швидкість навчання
- `--num-classes`: кількість класів

### 4. Оцінка моделі


python main.py --mode evaluate --checkpoint ./checkpoints/best_model.pth


## Структура даних

### Формат анотацій (JSON)

json
[
  {
    "image_path": "image_001.jpg",
    "boxes": [
      [x1, y1, x2, y2],
      [x1, y1, x2, y2]
    ],
    "labels": [1, 3]
  }
]


Де:
- `boxes`: координати bounding boxes у форматі [x1, y1, x2, y2]
- `labels`: ID класів об'єктів

### Структура датасету


data/
├── train/
│   ├── images/             
│   │   ├── image_001.jpg
│   │   ├── image_002.jpg
│   │   └── ...
│   └── annotations.json     
└── val/
    ├── images/
    │   └── ...
    └── annotations.json


##  Основні модулі

### 1. config.py
Містить всі налаштування системи:
- Параметри моделі
- Параметри навчання
- Шляхи до даних
- Параметри візуалізації

### 2. model.py
Реалізація Faster R-CNN:
- Завантаження попередньо навченої моделі
- Детекція об'єктів
- Збереження/завантаження чекпоінтів

### 3. visualizer.py
Візуалізація результатів:
- Малювання bounding boxes
- Побудова статистики детекцій
- Графіки розподілу класів

### 4. metrics.py
Обчислення метрик:
- **IoU (Intersection over Union)**
- **mAP (Mean Average Precision)**
- **Precision & Recall**
- **F1-score**

### 5. trainer.py
Навчання моделі:
- Тренувальний цикл
- Валідація
- Збереження чекпоінтів
- Візуалізація історії навчання

### 6. report_generator.py
Автоматична генерація звітів:
- Текстові звіти
- JSON звіти
- Візуальні звіти з графіками

## Метрики якості

### IoU (Intersection over Union)
Міра перекриття між передбаченим та справжнім bounding box:


IoU = Area of Overlap / Area of Union


### mAP (Mean Average Precision)
Середня точність по всіх класах при різних порогах IoU.

### Precision & Recall
- **Precision**: частка правильних детекцій серед усіх детекцій
- **Recall**: частка виявлених об'єктів серед усіх об'єктів

##  Приклади використання

### Приклад 1: Детекція на власному зображенні

python
from model import FasterRCNNDetector
from visualizer import DetectionVisualizer
from utils import load_image

# Ініціалізація
model = FasterRCNNDetector(pretrained=True)
visualizer = DetectionVisualizer()

# Завантаження зображення
image, image_tensor = load_image('my_image.jpg')

# Детекція
predictions = model.predict([image_tensor], threshold=0.5)

# Візуалізація
image_with_boxes = visualizer.draw_boxes(image, predictions[0])
visualizer.visualize_predictions(image, predictions[0], 
                                save_path='result.png')


### Приклад 2: Навчання на власному датасеті

python
from model import FasterRCNNDetector
from dataset import ObjectDetectionDataset, DatasetBuilder
from trainer import ModelTrainer

# Створення датасету
train_dataset = ObjectDetectionDataset(
    root_dir='./data/train',
    annotation_file='./data/train/annotations.json',
    mode='train'
)

train_loader = DatasetBuilder.build_dataloader(train_dataset)

# Ініціалізація моделі
model = FasterRCNNDetector(num_classes=10, pretrained=True)

# Навчання
trainer = ModelTrainer(model, train_loader)
history = trainer.train(num_epochs=10)

# Графіки
trainer.plot_training_history(save_path='training.png')


### Приклад 3: Обчислення метрик

python
from metrics import DetectionMetrics

# Ініціалізація
metrics = DetectionMetrics(num_classes=10, iou_threshold=0.5)

# Оцінка моделі
results = metrics.evaluate_model(predictions, ground_truths)

# Виведення результатів
print(f"mAP: {results['overall']['mAP']:.4f}")
print(f"Precision: {results['overall']['mean_precision']:.4f}")
print(f"Recall: {results['overall']['mean_recall']:.4f}")


### Приклад 4: Донавчання моделі

python
from trainer import ModelTrainer

# Завантаження попередньо навченої моделі
model.load_checkpoint('checkpoints/base_model.pth')

# Донавчання з замороженим backbone
history = trainer.fine_tune(num_epochs=5, freeze_backbone=True)


## Генерація звітів

Система автоматично генерує три типи звітів:

1. **Текстовий звіт** (`report.txt`):
   - Інформація про модель
   - Історія навчання
   - Метрики оцінки
   - Результати детекції

2. **JSON звіт** (`report.json`):
   - Структуровані дані для подальшої обробки

3. **Візуальний звіт** (`visual_report.png`):
   - Графіки навчання
   - Розподіл метрик по класах
   - Статистика детекцій

## Конфігурація

Основні параметри в `config.py`:

python
# Модель
NUM_CLASSES = 3
DETECTION_THRESHOLD = 0.5

# Навчання
LEARNING_RATE = 0.005
BATCH_SIZE = 4
NUM_EPOCHS = 10

# Пристрій
DEVICE = torch.device('cuda' if torch.cuda.is_available() else 'cpu')


## 🔍 Підтримувані формати

- **Зображення**: JPG, PNG, BMP
- **Анотації**: JSON (власний формат), COCO format
- **Моделі**: PyTorch checkpoint (.pth)

## Вихідні дані

Система зберігає результати в наступних директоріях:


output/
├── visualizations/     # Зображення з детекціями
├── predictions/        # Передбачення моделі
checkpoints/           # Збережені моделі
reports/              # Згенеровані звіти


##  Помилки та їх вирішення

### Проблема: CUDA out of memory
**Рішення**: Зменшіть `BATCH_SIZE` в конфігурації

### Проблема: Не знайдено датасет
**Рішення**: Програма автоматично створить тестовий датасет

### Проблема: Низька точність детекції
**Рішення**: 
- Зменшіть `DETECTION_THRESHOLD`




